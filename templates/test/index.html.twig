{% extends 'base.html.twig' %}

{% block title %}Hello TestController!{% endblock %}
{% block body %}
<div class="row">
    <div class="col">
        {% for item in tab %}
        {{item}}<br>
        {% endfor %}

    </div>
    <div class="col">
        {% for item in tab %}
        {{item}}<br>
        {% endfor %}

    </div>
    <div class="col">
        {% for item in tab %}
        {{item}}<br>
        {% endfor %}

    </div>

</div>
{% endblock %}